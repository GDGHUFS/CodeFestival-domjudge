<span class="badge text-bg-secondary id-badge" data-bs-toggle="tooltip" data-placement="top" title="{% if label is defined and label | length %}레이블: {{ label }}, {% endif %}{% if externalId is not null %}외부 ID: {{ externalId }}, {% endif %}내부 ID: {{ idPrefix }}{{ id }}">
    {% if label is defined and label | length %}
        {{ label }}
    {% elseif externalId is not null %}
        {{ externalId }}
    {% else %}
        {{ idPrefix }}{{ id }}
    {% endif %}
</span>
