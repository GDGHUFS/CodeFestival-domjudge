{% extends "jury/base.html.twig" %}
{% import "jury/jury_macros.twig" as macros %}

{% block title %}카테고리 {{ teamCategory.categoryid }} - {{ parent() }}{% endblock %}

{% block extrahead %}
    {{ parent() }}
    {{ macros.table_extrahead() }}
{% endblock %}

{% block content %}

    <h1>카테고리 {{ teamCategory.name }}</h1>

    <div class="row">
        <div class="col-lg-4">
            <table class="table table-sm table-striped">
                <tr>
                    <th>ID</th>
                    <td>{{ teamCategory.categoryid }}</td>
                </tr>
                {% if showExternalId(teamCategory) %}
                    <tr>
                        <th>외부 ID</th>
                        <td>{{ teamCategory.externalid }}</td>
                    </tr>
                {% endif %}
                <tr>
                    <th>ICPC ID</th>
                    <td>
                        {% if teamCategory.icpcid %}
                            {{ teamCategory.icpcid }}
                        {% else %}
                            -
                        {% endif %}
                    </td>
                </tr>
                <tr>
                    <th>정렬 순서</th>
                    <td>{{ teamCategory.sortorder }}</td>
                </tr>
                {% if teamCategory.color %}
                    <tr>
                        <th>색상</th>
                        <td style="background-color: {{ teamCategory.color }};">{{ teamCategory.color }}</td>
                    </tr>
                {% endif %}
                <tr>
                    <th>표시 여부</th>
                    <td>{{ teamCategory.visible | printYesNo }}</td>
                </tr>
                <tr>
                    <th>자가 등록 허용</th>
                    <td>{{ teamCategory.allowSelfRegistration | printYesNo }}</td>
                </tr>
            </table>
        </div>
    </div>

    {%- if is_granted('ROLE_ADMIN') -%}
        <p>
            {{ button(path('jury_team_category_edit', {'categoryId': teamCategory.categoryid}), '수정', 'primary', 'edit') }}
            {{ button(path('jury_team_category_delete', {'categoryId': teamCategory.categoryid}), '삭제', 'danger', 'trash-alt', true) }}
            {{ button(path('jury_team_category_request_remaining', {'categoryId': teamCategory.categoryid}), '남은 테스트케이스 채점 요청', 'secondary', 'gavel') }}
        </p>
    {% endif %}

    <h3>팀 목록</h3>
    {% if teamCategory.teams is empty %}
        <p class="nodata">팀 없음</p>
    {% else %}
        <div class="row">
            <div class="col-lg-4">
                <table class="data-table table table-sm table-striped table-hover">
                    <thead>
                    <tr>
                        <th style="text-align: right;">ID</th>
                        <th>팀 이름</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for team in teamCategory.teams %}
                        <tr>
                            <td style="text-align: right">
                                <a href="{{ path('jury_team', {'teamId': team.teamid}) }}">{{ team | entityIdBadge('t') }}</a>
                            </td>
                            <td>
                                <a href="{{ path('jury_team', {'teamId': team.teamid}) }}">{{ team.effectiveName }}</a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>

        <h3>제출물</h3>

        <div data-ajax-refresh-target>
            {%- include 'jury/partials/submission_list.html.twig' with {showTestcases: false} %}
        </div>
    {% endif %}

{% endblock %}
