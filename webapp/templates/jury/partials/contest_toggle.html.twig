<form action="{{ path('jury_contest_toggle', {'contestId': contest.cid, 'type': type}) }}" method="post" class="d-inline">
    <input type="checkbox" data-toggle="toggle" data-size="mini" data-on="예" data-off="아니요"
           name="value" {% if enabled %}checked{% endif %}>
</form>
