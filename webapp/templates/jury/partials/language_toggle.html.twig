<form action="{{ path(path, {'langId': language.langid}) }}" method="post" class="d-inline">
    <input type="checkbox" data-toggle="toggle" data-size="mini" data-on="예" data-off="아니요"
           name="value" {% if value %}checked{% endif %}>
</form>
