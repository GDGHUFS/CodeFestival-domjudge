{% extends "jury/base.html.twig" %}
{% import "jury/jury_macros.twig" as macros %}

{% block title %}언어 - {{ parent() }}{% endblock %}

{% block extrahead %}
    {{ parent() }}
    {{ macros.table_extrahead() }}
    {{ macros.toggle_extrahead() }}
{% endblock %}

{% block content %}

    <h1>활성화된 언어</h1>

    {{ macros.table(enabled_languages, table_fields) }}

    <br/>
    <br/>

    <h1>비활성화된 언어</h1>

    {{ macros.table(disabled_languages, table_fields) }}

    {% if is_granted('ROLE_ADMIN') %}
        <p>
            {{ button(path('jury_language_add'), '새 언어 추가', 'primary', 'plus') }}
        </p>
    {% endif %}

{% endblock %}

{% block extrafooter %}
    {{ macros.toggle_autosubmit_extrafooter() }}
{% endblock %}
